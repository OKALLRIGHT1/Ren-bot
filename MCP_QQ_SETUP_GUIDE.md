# MCP / QQ 接入说明

本文档对应当前仓库里已经落地的两条能力：

- 通过 `NapCat` 把 QQ 私聊 / 群聊消息接进桌面助手
- 通过 `MCP` 接入外部工具服务器，并在聊天里调用这些工具

如果你是在排查问题，优先看本文末尾的“常见问题”，也可以看 `docs/TROUBLESHOOTING.md`。

---

## 1. 现状概览

当前版本已经支持：

- QQ 消息接入主对话链路
- QQ 来源消息默认 **不驱动 Live2D / TTS**
- QQ 回复可选择是否同步显示到本地聊天框
- NapCat 参数可在设置页保存后 **尽量即时生效**
- MCP 本地工具桥
- 远程 MCP `stdio` / `streamable_http` 工具发现与调用
- 聊天里通过 `mcp_tools` 插件调用远程 MCP 工具
- QQ 图片识别和主人识别
- QQ 会话记忆隔离
- QQ 语音回复、截图回发、文件回发等插件能力

当前版本的 MCP 设计是：

- **稳优先**：每次列工具 / 调工具都会重新建立一次 MCP 会话
- 好处是状态干净、不容易挂住旧连接
- 代价是 `stdio` 服务首次调用会有一点启动开销

重要边界：

- QQ 来源默认不驱动桌面 Live2D / TTS。
- 如果你想让桌面 Live2D 说话，请用本地聊天框、语音输入或屏幕感知链路。
- 如果你想让 QQ 收到语音，请配置 QQ 概率语音回复或对应插件。

---

## 2. QQ / NapCat 接入

### 2.1 推荐方案

推荐使用 **QQ 副号 + NapCat**，不要直接拿主号测试。

推荐默认配置：

- 先只开私聊
- 群聊先关掉，确认稳定后再开
- 如果开群聊，建议保持“仅响应 @我”

推荐值：

- `NAPCAT_ALLOW_PRIVATE = 1`
- `NAPCAT_ALLOW_GROUP = 0`
- `NAPCAT_GROUP_REQUIRE_AT = 1`

### 2.2 设置页位置

打开设置后进入：

- `MCP / QQ`

主要项说明：

- `启用 NapCat Webhook 接入`
- `Webhook Host`
- `Webhook Port`
- `Webhook Path`
- `Access Token`
- `NapCat API Base`
- `NapCat API Token`
- `允许副号回发消息`
- `允许私聊`
- `允许群聊`
- `群聊仅响应 @我`
- `QQ 回复同步显示到本地聊天框`

### 2.3 一组推荐值

如果你的 Live2D 助手和 NapCat 在同一台机器上，推荐先这样填：

- `Webhook Host`：`127.0.0.1`
- `Webhook Port`：`8095`
- `Webhook Path`：`/chat/napcat`
- `NapCat API Base`：`http://127.0.0.1:3000`

这样 webhook 地址通常就是：

- `http://127.0.0.1:8095/chat/napcat`

### 2.4 NapCat 侧需要做什么

你需要在 NapCat / OneBot 上报设置里，把消息接到本程序开放的入口。

当前版本已经改成 **同端口同时支持两种入站方式**：

- `HTTP webhook`
- `反向 WebSocket`

也就是说，你现在可以二选一：

- `HTTP` 上报地址：`http://127.0.0.1:8095/chat/napcat`
- `反向 WS` 地址：`ws://127.0.0.1:8095/chat/napcat`
- 如果你那边只方便填根地址，也兼容：`ws://127.0.0.1:8095`

更直白一点说，当前这套接法建议这样理解：

- 你的桌面助手这边：现在是 **QQ 消息网关**，同端口同时收 `HTTP` 和 `WS`
- NapCat 那边：可以继续用 **客户端 / HTTP 上报模式**，也可以改成 **反向 WebSocket Client**
- 程序回 QQ 时：**优先走反向 WS**；如果当前没有可用 WS 连接，则回退到 `NapCat API Base`
- 所以如果你只用 `HTTP webhook` 入站，NapCat 自己的 **HTTP API 服务** 仍然建议保持开启

如果你配置了 `Access Token`，NapCat 上报时也要带同样的 token。

如果你之前看到过：

- `Unexpected server response: 501`

那通常就是把 NapCat 配成了反向 `WS`，但程序端当时还只开着 `HTTP`。当前版本已经补上这个兼容层。

### 2.5 当前 QQ 消息策略

当前程序对 QQ 来源消息的行为是：

- 会进入主对话链路
- 会照常走记忆 / 工具 / LLM
- 会正常回发到 QQ
- **不会驱动 Live2D 气泡**
- **不会驱动 TTS 发声**

这就是你之前要求的：

- QQ 发来的消息和电脑本地输入要分流
- QQ 消息不要让 Live2D 在桌面上“跟着说出来”

如果你发现 QQ 消息让桌面形象动了，优先检查该消息是否其实来自本地输入、屏幕感知或某个插件主动输出，而不是 NapCat 入站。

### 2.6 QQ 消息文本清洗

NapCat 上报如果是消息段数组，程序会尽量清洗成干净文本再给模型。

例如：

- 图片 -> `[图片]`
- 文件 -> `[文件]`
- 视频 -> `[视频]`
- 语音 -> `[语音]`
- 卡片消息 -> `[卡片消息]`

群聊里 `@机器人` 的段也会被自动剥离，不会把脏 CQ 码直接喂给模型。

### 2.7 主人识别与 QQ 图片识别

当前版本已经额外支持两项更实用的能力：

- 在设置页填写“主人 QQ”后，助手会区分：
  - 这是你本人发来的消息
  - 这不是你，而是其他联系人 / 群成员
- QQ 消息里带图片时，程序会先尝试做图片理解，再把图片内容一起喂给模型

设置页新增项都在：

- `MCP / QQ`

新增字段说明：

- `主人 QQ`
  - 填你自己的 QQ 号
  - 支持多个，适合你有主号 / 副号同时测试
  - 多个 ID 用逗号、空格或换行分隔都可以
- `主人称呼`
  - 默认是 `主人`
  - 这里只影响注入给模型的身份描述
- `启用 QQ 图片识别`
  - 开启后，QQ 图片会走视觉模型分析
  - 关闭后，模型只会看到普通占位文本，比如 `[图片]`
- `图片提示词`
  - 用来控制图片识别时更关注什么
  - 例如你可以改成“重点识别聊天截图里的文字内容”
- `QQ 过滤模式`
  - 支持三种：`不过滤` / `白名单` / `黑名单`
  - 白名单模式下，只有命中的用户 / 群才会进入助手
  - 黑名单模式下，命中的用户 / 群会被直接拦掉
- `用户白名单 / 用户黑名单`
  - 这里填 QQ 号
  - 私聊会按发送者 QQ 号过滤
  - 群聊也可以按群成员 QQ 号过滤
- `群白名单 / 群黑名单`
  - 这里填群号
  - 群聊会按群号过滤

当前行为细节：

- 如果发送者 QQ 在 `主人 QQ` 列表里：
  - 模型会被明确告知“这条消息来自主人本人”
- 如果不在：
  - 模型会被明确告知“这不是主人，是其他 QQ 联系人或群成员”
- QQ 图片识别结果会作为额外上下文拼进这次对话
- QQ 来源消息依然**不会驱动 Live2D / TTS**
- QQ 对话默认**沿用当前本地激活人设**，不会切成单独的“QQ人格”

过滤规则说明：

- 私聊：按 `用户名单` 过滤
- 群聊：按 `群名单` 和 `用户名单` 共同过滤
- 白名单模式：
  - 私聊需要发送者 QQ 在用户白名单里
  - 群聊只要“群号在群白名单”或“发送者 QQ 在用户白名单”即可通过
- 黑名单模式：
  - 私聊如果发送者 QQ 在黑名单里则拦截
  - 群聊如果“群号在群黑名单”或“发送者 QQ 在用户黑名单”则拦截


补充（2026-03-12）：

- 支持在插件 `access_control` 里配置 `allow_group_without_at`，该插件在群聊可免 @ 触发（仅对该插件生效）。

### 2.8 QQ 会话记忆隔离

当前版本已经把 QQ 记忆改成按 `session_id` 分桶：

- 私聊：按 `private:<QQ号>` 隔离
- 群聊：按 `group:<群号>` 隔离

现在生效的隔离范围包括：

- 短期上下文
- transcript 兜底回忆
- 向量记忆检索

这意味着：

- A 联系人的私聊内容，不会直接串到 B 联系人的私聊里
- 某个群里的上下文，不会直接串到另一个群
- 本地桌面聊天默认也不会再把 QQ 分会话内容当成同一个短期上下文继续接着聊

当前设计说明：

- 这是自动生效的，不需要额外开关
- 旧的历史数据里如果原本没有 `session_id`，它们还是老的“全局数据”
- 新写入的 QQ 对话会自动带上会话标识

建议你一开始先这样配：

- `主人 QQ`：填你主号和测试副号
- `主人称呼`：先保持 `主人`
- `启用 QQ 图片识别`：打开
- `图片提示词`：先用默认值，等稳定后再细调

---

## 3. MCP 接入

### 3.1 当前支持的传输

当前已经支持两种 MCP 服务器配置：

- `stdio`
- `streamable_http`

### 3.2 设置页配置方式

打开设置页里的：

- `MCP / QQ`

现在不需要手填 JSON 了，直接在设置页里：

- 点 `+ 本地进程`：配置 `stdio` 类型 MCP
- 点 `+ HTTP 服务器`：配置 `streamable_http` 类型 MCP
- 用 `编辑所选 / 删除所选 / 上移 / 下移` 管理服务器列表

常见字段在 GUI 里已经拆开：

- `名称`
- `连接方式`
- `启动命令 / 启动参数 / 工作目录 / 环境变量`
- `URL / 请求头`
- `启用状态`
- `超时时间`

也就是说，平时只需要填表单，不需要再自己拼 JSON 结构。

这些配置最终仍然会保存到：

- `data/runtime_settings.json`

对应字段：

- `mcp_server_configs`

### 3.3 `stdio` 配置示例

```json
[
  {
    "name": "filesystem",
    "transport": "stdio",
    "command": "python",
    "args": ["-m", "your_mcp_server"],
    "cwd": "D:/path/to/server",
    "enabled": true,
    "timeout_sec": 20
  }
]
```

字段说明：

- `name`：服务器名
- `transport`：固定写 `stdio`
- `command`：启动命令
- `args`：命令参数数组
- `cwd`：可选，工作目录
- `enabled`：是否启用
- `timeout_sec`：初始化 / 列工具 / 调工具超时

### 3.4 `streamable_http` 配置示例

```json
[
  {
    "name": "remote_tools",
    "transport": "streamable_http",
    "url": "http://127.0.0.1:8000/mcp",
    "headers": {
      "Authorization": "Bearer your-token"
    },
    "enabled": true,
    "timeout_sec": 20,
    "sse_read_timeout_sec": 300
  }
]
```

字段说明：

- `url`：MCP 的 streamable HTTP 地址
- `headers`：可选，请求头
- `sse_read_timeout_sec`：长连接读取超时

### 3.5 保存后会发生什么

点击保存后，程序会：

- 先刷新本地 MCP 工具桥
- 再尝试连接远程 MCP 服务器
- 拉取它们暴露的工具列表
- 把这些工具注册成统一名字

远程工具的统一命名格式是：

- `mcp.<server>.<tool>`

例如：
- `mcp.filesystem.read_file`
- `mcp.remote_tools.search`

---

## 4. QQ 概率语音回复

当前版本已经支持：

- QQ 回消息时按概率改为发送语音
- 命中概率后，优先发送 QQ 语音
- 如果语音合成失败，或语音消息发送失败，会自动回退成普通文字回复
- 该能力可在 GUI 的 `MCP / QQ` 页面直接配置，不需要手改 JSON

设置项：

- `启用 QQ 概率语音回复`
- `语音概率`

当前行为说明：

- 只对 QQ / NapCat 回发链路生效，不影响本地桌面聊天的显示逻辑
- 语音内容使用当前本地 TTS 后端临时合成
- 命中后默认发送语音消息，不额外再发一条同内容文字；如果语音失败，才回退文字

建议：

- 初次使用可以先从 `15% ~ 30%` 开始
- 如果你主要拿她做日常聊天，`20%` 一般比较自然
- 如果你希望更像真人副号，可以再提高到 `35% ~ 50%`

注意：

- 当前实现通过本地临时音频文件给 NapCat / OneBot 发 `record` 段
- 因此更推荐 **NapCat 和桌面助手运行在同一台机器上**
- 如果你的 NapCat 不在本机，语音消息可能无法直接读取这个临时文件

## 5. 聊天里怎么调用 MCP

当前仓库里新增了一个插件：

- `MCP 工具桥`
- 触发命令：`mcp_tools`
- 插件类型：`delegate`，正常聊天会先由副脑判断并调用；下面的 `[CMD: ...]` 主要用于调试或说明实际调用格式。

### 5.1 查看当前可用 MCP 工具

```text
[CMD: mcp_tools | list_tools]
```

### 5.2 查看当前远程服务器状态

```text
[CMD: mcp_tools | server_status]
```

### 5.3 调用某个远程 MCP 工具

```text
[CMD: mcp_tools | call_tool ||| mcp.filesystem.read_file ||| {"path": "README.md"}]
```

注意：

- 第 2 段必须是工具名
- 第 3 段必须是 JSON 对象

如果你只是在设置好之后想让模型自己调用，聊天系统也会把当前远程 MCP 工具清单注入到提示词里。

---

## 6. 当前程序里的 MCP 本地管理工具

除了远程工具本身，程序还内置了几个管理用 MCP 工具：

- `mcp.list_tools`
- `mcp.server_status`
- `mcp.call_tool`

这些主要是给调试和后续联动留的统一入口。

---

## 7. 推荐联调顺序

建议按下面顺序来：

### 第一步：先通 QQ

- 开 NapCat
- 只开私聊
- 能收到 QQ 消息
- 能正常回发到 QQ

### 第二步：再通本地 MCP

- 先配置 1 个最简单的 `stdio` MCP server
- 保存后看设置页提示是否发现工具
- 用 `mcp_tools | list_tools` 看有没有远程工具名
- 手工调用 1 次 `call_tool`

### 第三步：最后再开群聊

- 打开 `允许群聊`
- 保持 `群聊仅响应 @我`
- 先在小群里测

---

## 8. 常见问题

### 8.1 为什么 QQ 消息来了但桌面角色不说话？

这是当前设计，属于故意分流：

- QQ 来源消息不会驱动 Live2D / TTS

### 8.2 为什么保存 MCP 后还是没有工具？

先检查：

- JSON 是否合法
- `command` / `args` 是否能独立启动 server
- `url` 是否正确
- token / headers 是否正确
- 远程 server 自己是否真的暴露了 tools

### 8.3 为什么 MCP 工具名要带 `mcp.<server>.` 前缀？

因为这样最稳：

- 不会和本地工具重名
- 多个远程 server 也不容易撞名
- 聊天里更容易看出工具来源

### 8.4 为什么现在每次调用都会重新连一次 MCP？

这是当前实现的取舍：

- 优点：更稳，旧连接坏了也不容易拖死整条链路
- 缺点：`stdio` 工具第一次调用会慢一点

如果你后面确认常用 server 很稳定，可以再把它改成常驻会话池。

---

## 9. QQ 远程截图

- 现在已新增一个 `QQ远程截图` 插件。
- 作用：你在 QQ 里明确说“截图发我”“给我看看屏幕”这类话时，助手会直接把当前电脑屏幕截图发回 QQ。
- 现在还支持：
  - 主屏：`主屏截图发我`
  - 全部屏幕：`全部屏幕发我`
  - 指定屏幕：`第2屏截图发我`
  - 去掉标题：`截图发我，不带标题`
- 默认权限：
  - 仅 QQ 主人可用
  - 其他联系人默认不可用
- 默认不会影响本地桌面的“看屏幕/截图分析”插件，两者是分开的：
  - `vision`：偏“看图分析”
  - `qq_screenshot`：偏“直接把截图发回 QQ”
- 如果你想改默认截图范围，不用改 JSON：
  - 打开 `设置 -> 插件管理 -> 编辑 -> QQ远程截图`
  - 可直接修改：
    - `默认截图范围`
    - `默认屏幕序号`
    - `附带当前窗口标题`
- 如果你新增完后没看到效果：
  - 重启程序，或到插件管理里刷新插件列表
  - 确认 NapCat 回发本来就是正常的
  - 再用明确短句测试：`截图发我`

## 10. MCP 自然语言调用（2026-03-08 新增）

如果你希望“查一下麦当劳优惠券”这类话优先走 MCP，而不是误触发联网搜索，现在已经支持配置化路由，不需要改代码。

### 10.1 在哪里改

- `设置 -> MCP -> MCP 自然语言路由`

可直接改：
- `启用 MCP 自然语言优先路由`
- `品牌词`
- `动作词`
- `联网覆盖词`

### 10.2 当前路由规则

- 同时命中：`品牌词 + 动作词` => 优先路由 `mcp_tools`
- 如果同时命中“联网覆盖词” => 不抢占 MCP，优先允许走联网搜索

### 10.3 典型效果

- `我今天吃了麦当劳`：不触发 MCP
- `查一下麦当劳优惠券`：优先触发 MCP
- `联网查一下麦当劳优惠券`：优先联网搜索

### 10.4 新增 MCP 时是否还要改代码

不需要。

新增 MCP 或新增业务域时，只要在 GUI 增加对应关键词即可。底层会从 `plugins/mcp_tools/config.json -> settings` 动态读取。


---

## 11. QQ 固定命令生图（Grok / 兼容图片接口）

如果你想在 QQ 里用固定命令触发生图，现在可以直接用内置的 `qq_draw` 插件。

当前行为：

- 只响应 QQ 来源消息
- 只响应固定前缀：`/画图` 或 `/画画`
- 前缀后面的整段文本会作为提示词
- 插件现在会直连兼容 OpenAI 风格的图片接口
- 默认只允许 QQ 主人触发
- 默认允许群聊免 `@`，因为斜杠命令已经足够明确

### 11.1 先决条件

你需要准备一个可用的生图接口，例如：

- Grok / xAI 的兼容图片接口
- 其他任意兼容 OpenAI 风格 `images/generations` 的中转接口

推荐你先确认：

- 接口支持 `POST /v1/images/generations` 之类的图片生成请求
- 返回里能拿到 base64 图片，或者可下载的图片 URL

### 11.2 插件默认配置

插件文件：

- `plugins/qq_draw/config.json`

默认关键项：

- `base_url = https://wzw.pp.ua`
- `api_mode = chat` 或 `images`
- `endpoint_path = /v1/chat/completions` 或 `/v1/images/generations`
- `api_key = <你的 key>`
- `model_name = grok-imagine-1.0` 或该站真实图片模型名
- `size_value = 1024x1024`
- `debug_logging = false`（需要排查时再打开）

建议：

- `API Key` 尽量填到 GUI 配置里，或者放环境变量 `GROK_API_KEY`
- 不要把 key 直接写进仓库文档或提交到 git
- `额外请求体(JSON)` 默认建议先留空；如果你要填 `{"response_format":"b64_json"}`，插件现在会自动转成部分兼容站要求的对象格式
- 现在插件编辑器会对 `API Key / token / secret / password` 类字段做遮挡显示，便于前端修改但不直接明文暴露
- 保存后真实密钥会进入本地 SQLite：`data/plugin_secrets.db`；插件目录里的 `config.json` 会保留空默认值，方便新增插件和版本更新

如果你的中转站把生图模型挂在聊天接口，就把 `接口模式` 改成 `chat`，并把 `接口路径` 改成 `/v1/chat/completions`。

### 11.3 QQ 里怎么用

直接发：

```text
/画图 绘制一个丰川祥子在雨后的街道
```

或者：

```text
/画画 画一个日系动画风的深夜电车站
```

注意：

- 必须以 `/画图` 或 `/画画` 开头才会触发
- 不是前缀命令时，即便消息里提到“画图”两个字，也不会触发
- 这样就是为了避免普通聊天误触发

### 11.4 接口返回格式要求

当前 `qq_draw` 默认会从接口返回里尝试提取这些字段：

- `image_base64`
- `base64`
- `data`
- `b64_json`

也支持：

- 图片 URL，例如 `data[0].url`
- chat/completions 流式返回中的 `data: {...}` 片段；插件会自动尝试合并并提取图片字段

如果你的接口返回的是别的结构：

- 要么把服务端返回改成上述字段之一
- 要么扩展 `plugins/qq_draw/plugin.py` 里的解析逻辑

### 11.5 推荐联调顺序

- 先确认 QQ 收发正常
- 再在插件管理里把 `QQ生图` 的 `API Base URL`、`API Key`、`模型名` 填对
- 如果接口路径不是默认值，再把 `图片接口路径` 改对
- 最后在 QQ 私聊测试：`/画图 你的提示词`

### 11.6 常见问题

- QQ 发了 `/画图` 没反应：
  - 先确认你是 QQ 主人账号
  - 再确认插件 `QQ生图` 没被禁用
  - 再确认 NapCat 回发本来就是正常的
- 提示调用接口失败：
  - 检查 `base_url`、`endpoint_path`、`api_key` 是否正确
  - 检查你的中转站是否真的支持图片生成接口
  - 如果报 `response_format` 相关错误，先把 `额外请求体(JSON)` 清空再测
- 如果报 `Image rate limit exceeded`，说明额度或频率限制触发了，先换 key 或稍后再测
- 如果你想看接口原始返回，可在 `QQ生图` 里打开 `调试日志`，控制台会打印请求和响应摘要
- 提示“没解析出图片”：
  - 说明接口调用成功了，但返回结构不是当前插件识别的 base64 字段或 URL
  - 这时需要调整服务端返回，或扩展插件解析逻辑

---

## 12. QQ 链接卡片（share）

当你在 QQ 里明确要链接（例如“给我 xx 链接 / 网址”），助手会尝试发送链接卡片，而不是只回纯文本。

---

## 13. QQ 私聊定时提醒

现在可以直接在 QQ 私聊里创建定时提醒。

示例：

```text
/提醒 每周1到周5 17:20 提醒我打卡
```

也支持：

```text
/提醒 工作日 五点二十 提醒我打卡
```

常用命令：

- `/提醒列表`
- `/提醒删除 1`
- `/提醒测试 1`

规则：

- 只建议在 QQ 私聊里创建
- 到点后只发给你本人，不发群里
- 提醒数据保存在 `data/qq_reminders.json`

---

## 14. 魔法日报立即测试推送

现在 `魔法日报` 支持立即测试：

- 在任意 QQ 会话里发送包含 `测试推送` 或 `立即推送`
  - 会向 `scheduled_push_targets` 执行一次测试推送
- 在当前会话发送包含 `测试当前`
  - 会把日报直接测试发到当前 QQ 会话

适合排查：

- `scheduled_push_targets` 是否配置正确
- 当前 QQ 网关发送是否正常
- 图片渲染失败时是否能自动回退文字版

---

## 15. QQ 文件白名单目录

`QQ文件浏览` 现在支持通过插件设置增加白名单目录。

配置项：

- `额外挂载目录`

格式：

```text
qqsave|D:\QQDownloads
```

表示把本地目录挂载为虚拟目录 `/qqsave`。

配置后在 QQ 中可用：

```text
/cd /qqsave
/ls
/get 1
```

如果你要把当前 QQ 下载目录加入白名单，就把真实本地路径填进去即可。

---

## 16. QQ 功能帮助

现在可以直接在 QQ 里问她当前有什么功能。

例如：

```text
/帮助
```

也支持按类别查：

```text
/帮助 生图
/帮助 提醒
/帮助 文件
```

或者直接说：

```text
你有什么功能
```

它会返回：

- 常用 QQ 指令
- 当前已加载的 direct 插件
- 每个插件常用命令和说明

---

## 17. QQ 角色切换

现在可以直接在 QQ 中切换角色。

例如：

```text
/角色列表
/角色 当前
/角色 丰川祥子
```

也支持直接发：

```text
/丰川祥子
```

切换后会同步：

- 角色提示词
- 角色专属 GPT-SoVITS 配置（如果已启用）
- 该角色当前默认服装（如果已配置）

---

## 18. QQ 语音回复附带文本

现在如果 QQ 触发了语音回复：

- 会先发语音
- 再附带发送一份文本

这样你在 QQ 里既能听，也能直接看文字内容。

---

## 20. 联网搜索本地/远程双接口

`联网搜索` 现在支持同时配置两个 Exa 地址：

- `Exa 本地地址`
- `Exa 远程地址`

并且可以设置：

- `优先本地接口`

工作方式：

- 开启后先尝试本地 Exa
- 本地失败再自动尝试远程 Exa
- 两者都失败才回退 DDG
- 在插件管理里可点击 `检测搜索接口`，快速判断本地和远程接口哪一条当前可用

---

## 21. 现在如何让她学习知识库

当前项目里，本地知识库通过 `knowledge_base` 插件学习和搜索。

如果你不想手打命令，现在也可以直接在主页打开：

- `更多功能 -> 知识库管理`

然后：

- 添加知识目录
- 点“一键学习”
- 再在下方直接搜索验证
- 也可以查看当前知识片段数，或执行“清空并重建”
- 现在还能对每个目录单独开关；关闭后不会参与新的 GUI 学习
- 如果你想让某个目录的旧知识马上失效，可以选中目录后点击“删除选中目录知识”
- 如果你以前用旧脚本 `ingest_knowledge.py` 导入过知识，旧目录 `knowledge_docs` 会自动出现在新知识库管理窗口里

### 学习一个目录

```text
knowledge_base ||| learn ||| D:\你的资料目录
```

它会递归扫描目录下这些文件：

- `.md`
- `.txt`
- `.py`
- `.json`

### 搜索知识库

```text
knowledge_base ||| search ||| 你的查询词
```

例如：

```text
knowledge_base ||| search ||| QQ 网关配置
```

### 当前行为说明

- 学习后的内容进入本地知识库向量集合
- 后续普通对话中，系统会自动把相关知识片段补进 prompt
- 现在已经补了“历史知识注入清洗”，不会像以前那样更容易把旧知识块一轮轮叠进上下文
- 如果你的数据是宝可梦图鉴/招式/特性这类结构化 JSON，系统现在会自动用专用适配器生成更适合检索的摘要块与关系块，不会只按换行硬切
- 如果你的 embedding 走远程接口，建议开启 `慢速导入模式`，大批量学习时会每处理一批文件自动暂停，减少 RPM 限频
- 如果仍然经常限频，可以继续开启 `自适应慢速模式`，系统会在限频出现时自动把暂停时间再拉长

---

## 22. 每日总结中的 QQ 数据

现在每日总结/日记在汇总主人聊天记录时，会更明确地区分：

- 本地聊天
- QQ 私聊
- QQ 群聊

也就是说，如果你当天主要在 NapCat QQ 群里发言，后续新生成的总结会更容易把这部分写进去，而不是误判成你没怎么用 QQ。

---

## 23. Rust 桌面采集 sidecar

当前项目支持一个 Rust sidecar 负责结构化桌面活动采集。

它会采集：

- 前台应用
- 窗口标题
- 基础浏览器上下文（初版）
- active / idle / locked 状态

并通过：

- `POST /gui/activity-ingest`

把事件回传给 Python 主程序。

### 启动方式

先在项目根目录构建：

```bash
cargo build --release --manifest-path rust-activity-agent/Cargo.toml
```

生成后主程序会在启动时自动尝试拉起：

```text
rust-activity-agent/target/release/rust-activity-agent.exe
```

退出主程序时也会一并关闭。

---

## 24. 角色切换与语音联动（2026-03 补充）

现在角色切换已经打通到：

- QQ 指令切换角色
- 形象管理里激活角色
- 程序启动时同步当前激活角色

统一联动内容：

- 角色提示词
- 角色专属 GPT-SoVITS 配置
- 角色当前默认服装
- 桌面 GUI 角色状态显示

同时，QQ 语音回复现在会附带一份文本，便于查看内容与存档。

行为规则：

- 链接来源优先级：
  - 回复文本中包含 URL 时，取第一条 URL
  - 如果文本里没有 URL，会尝试从工具结果里提取
- 成功发送卡片后：
  - QQ 会收到分享卡片
  - 文本会简化为“已发送链接卡片。”以避免重复堆 URL
  - QQ 来源依然不触发 Live2D / TTS（沿用现有策略）

---

## 25. 常见问题

### 25.1 NapCat 连不上

- 先确认程序日志里出现 `NapCat gateway listening`。
- 如果使用 HTTP webhook，地址通常是 `http://127.0.0.1:8095/chat/napcat`。
- 如果使用反向 WS，地址通常是 `ws://127.0.0.1:8095/chat/napcat`。
- 如果看到 `Unexpected server response: 501`，通常是 WS/HTTP 模式填反了，或旧版本只开了 HTTP。

### 25.2 QQ 收得到但不回

- 检查 `允许私聊` / `允许群聊`。
- 群聊检查 `群聊仅响应 @我`。
- 检查白名单 / 黑名单。
- 检查发送者是否在 `主人 QQ` 中，部分插件默认只允许主人触发。

### 25.3 QQ 回文字但不发语音

- QQ 语音是概率触发，不是每条都发。
- 检查 TTS 是否能本地合成。
- 检查 NapCat 是否允许发送语音文件。
- 查看日志里语音合成和 `send_voice` 的错误摘要。

### 25.4 MCP 工具找不到

- 确认 `mcp_tools` 插件已启用。
- 确认 `MCP / QQ` 页面中的服务器配置启用。
- 保存后看日志是否成功列出远程工具。
- 如果是 `stdio`，先单独在终端运行启动命令，确认不会卡住。

### 25.5 MCP 调用很慢

- 当前设计是稳优先，每次列工具 / 调工具会重新建立会话。
- `stdio` 服务首次调用慢是正常现象。
- 需要低延迟时，后续应做连接复用和健康检查，但这会提高状态管理复杂度。

